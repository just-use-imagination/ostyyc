# Background Image

Place your background image here as `background.jpg`.

The image should be:
- High resolution (1920x1080 or larger recommended)
- Optimized for web (JPEG format, compressed)
- Named exactly: `background.jpg`

If no image is provided, the app will use a dark gradient fallback.

